spring:
  datasource:
    url: ${DEV_JDBC_POSTGRES_DB}
    username: ${DEV_POSTGRES_USERNAME}
    password: ${DEV_POSTGRES_PASSWORD}
  data:
    redis:
      host: Localhost
      port: 6379
      password: ${DEV_REDIS_PASSWORD}
